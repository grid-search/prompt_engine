defmodule PromptEngineWeb.DashboardLive do
  # This one line injects all the helpers and configuration defined above.
  use PromptEngineWeb, :live_view

  # ... your LiveView code (mount, render, handle_event, etc.)
end
